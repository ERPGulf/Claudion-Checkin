/**
 * TEMPORARY — pre-redesign Home components, restored from git so the
 * "New Home Experience" toggle can render the old UI side by side with the new
 * one. Delete this whole folder (and `screens/HomeLegacy.jsx`) when the
 * experiment ends. See settings/homeExperience.js for the removal checklist.
 */
import WelcomeCard from './WelcomeCard';
import QuickAccess from './QuickAccess';
import LavaMenu from './YourLavas';

export { WelcomeCard, QuickAccess, LavaMenu };
